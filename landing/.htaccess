<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Download counter only: the rest of the API stays on its own vhost, so the
    # landing origin never exposes admin or auth endpoints.
    RewriteRule ^api/downloads$ api-proxy.php [QSA,L]
</IfModule>

Options -Indexes
